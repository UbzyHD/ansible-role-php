---
- name: install.yml | Installing PHP{{ php_version }}
  apt:
    name: php{{ php_version }}
    update_cache: true
    autoremove: true
    autoclean: true
    install-recommends: false
    state: present
